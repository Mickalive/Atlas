{
  "name": "atlas-renewal-audit",
  "version": "0.1.0",
  "private": true,
  "description": "Atlas — Atlassian Renewal FinOps / License Cost Optimizer (read-only V1). Read-only renewal audit producing ESTIMATED ANNUAL SAVINGS from defensible evidence.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck:backend": "tsc -p tsconfig.json --noEmit",
    "typecheck:frontend": "tsc -p tsconfig.frontend.json --noEmit",
    "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
    "lint": "node scripts/static-gates.mjs && node scripts/control-workflow-gates.mjs && node scripts/marketplace-readiness-gate.mjs",
    "audit:high": "npm audit --audit-level=high",
    "build": "npm run lint && npm run typecheck",
    "fixture:scan": "npx tsx src/dev/runFixtureScan.ts",
    "parity:local": "bash .github/scripts/forge-parity-check.sh"
  },
  "dependencies": {
    "@forge/api": "^8.0.4",
    "@forge/bridge": "^6.3.0",
    "@forge/kvs": "^2.0.4",
    "@forge/react": "^12.1.2",
    "@forge/resolver": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.23.12",
    "typescript": "~5.9.3",
    "vitest": "^4.1.11",
    "yaml": "^2.9.0"
  }
}
